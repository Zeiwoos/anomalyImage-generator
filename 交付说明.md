# Pipeline V2 客户交付说明

交付日期：2026-08-11  
版本：V2 改良版

## 本次交付包含

- 工业异常生成 Pipeline 与命令行工具；
- 异常质量审核和 Mask 审核一体化网页；
- LabelMe 多 ROI 扫描、独立审核和最终分层合成；
- 视觉 LLM 批量规划、参考选择、语义 Mask 和保护区；
- 下一张图片规划与当前 CORE 生成的单步前瞻并行；
- `gpt-image-2` Image Edits CORE 适配器；
- 程序 QC、视觉语义质检、失败原因和反馈驱动重试；
- 标签知识库和允许交付的异常参考库；
- 便携 Python 查找、项目内虚拟环境安装和启动脚本；
- 离线自动化测试；
- 完整流程分析与 CORE 接入说明。

## 本次交付不包含

- 客户或开发方业务数据集；
- API Base URL、API Key、Token 或本机 Claude 配置；
- 固定 Python、Conda 或用户目录路径；
- SQLite 数据库、审核结果 CSV、生成候选、中间 Prompt 和日志；
- `.venv`、`__pycache__`、`.pyc`；
- 演示 PPT 和演示视频。

## 首次使用

```bat
setup_env.bat
```

编辑 `config.json`，填写 `dataset.root`，然后执行：

```bat
start_review_tool.bat --check
run_pipeline.bat scan
start_review_tool.bat
```

打开审核台后，在“API 配置”中填写客户自己的 Base URL 和 API Key，并先执行连接测试。

## 默认性能策略

- CORE：`gpt-image-2`；
- 默认质量：`low`，可在网页切换为 `high`；
- 每个 ROI 每轮 1 个候选；
- 质量失败自动反馈重试 1 次；
- 同图最多 4 个 ROI 共用一次批量视觉规划；
- 启用下一图视觉规划单步前瞻，不并发两个 CORE 生图任务；
- 多 ROI 默认按前序成功异常构建后序生成上下文，最终仍从原图按各自精细 Mask 重组。

## 安全要求

`api_credentials.local.json`、`python_path.local.txt` 和 `中间产物/` 均为客户本机文件，不应对外分享。若需要问题诊断，应先对 Prompt、路径、图片和日志进行业务脱敏。

## 主要文档

- `README.md`：安装、扫描、运行、审核和导出；
- `CORE_INTEGRATION.md`：图像编辑接口和适配器；
- `V2完整生成流程分析.md`：LLM、参考图、Mask、CORE、多 ROI、重试和导出调用链；
- `性能优化说明.md`：快速平衡模式与并行规划；
- `PORTABLE_PACKAGE.md`：迁移和脱敏边界。
