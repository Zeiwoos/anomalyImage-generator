# 生成性能配置

当前默认采用“快速平衡模式”，目标是避免一个 ROI 在无人值守时连续消耗几十分钟。

一次 ROI 尝试现在最多包含：

1. 一次视觉 LLM 规划；
2. 一次 CORE 生图；
3. 一次视觉语义质检。

候选未通过时，程序会保存具体失败意见，并从干净源图进行一次反馈驱动的自动重试；第二次仍未通过则进入人工审核，不再盲目循环。与旧配置相比，默认远程请求由每轮最多 8 次降为 3 次，单次启动的自动重试由 8 次降为 1 次。

主要默认值：

```json
{
  "generation.max_auto_retries_per_sample_per_run": 1,
  "intelligence.candidate_count": 1,
  "intelligence.comparative_critic": false,
  "intelligence.planner_reasoning_effort": "high",
  "intelligence.critic_reasoning_effort": "medium",
  "intelligence.reference_candidate_count": 3,
  "intelligence.critic_reference_count": 1,
  "intelligence.transport_retries": 2,
  "core.transport_retries": 2,
  "core.transport_job_retries": 1
}
```

如果后续希望提高候选多样性，可将 `candidate_count` 改为 2，并保留 `stop_after_first_passing_candidate=true`。这样第一个候选通过后会立即停止，只有失败时才生成第二个候选。只有确实需要横向择优时才打开 `comparative_critic`，否则会额外增加一次视觉 LLM 请求。

配置修改后必须重启审核服务，正在运行的 Python 进程不会自动载入新代码。

## 视觉规划与 CORE 流水并行

默认启用 `intelligence.parallel_planning=true`。第一组样本完成视觉规划后，队列进入单步前瞻流水线：

- 当前 ROI 由 CORE 生图并接受语义质检；
- 独立视觉客户端同时分析下一张图片或下一组无依赖 ROI；
- 下一组轮到 CORE 时直接使用已经准备好的计划；
- `sequential_success` 模式下，同一图片后续分组如果依赖当前合成结果，则不会提前规划。

并行只增加一个视觉规划槽，不会并发启动两个 CORE 生成任务。UI 顶部进度和“进行中”侧栏会分别显示当前 CORE 阶段以及下一组的并行规划状态。若网关对并发请求限流，可设置 `intelligence.parallel_planning=false` 恢复串行。
